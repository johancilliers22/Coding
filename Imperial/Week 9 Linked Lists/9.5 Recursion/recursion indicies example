#include <iostream>
#include <fstream>
#include <cstdlib>
#include <vector>

double mpow(double x, int n){
    if(n == 0){
        return 1; // base case 
    }
    else{
        return x * mpow(x, n - 1); // recursive call
    }
}

int main(){
    std::cout<<mpow(5, 2)<<std::endl;
}