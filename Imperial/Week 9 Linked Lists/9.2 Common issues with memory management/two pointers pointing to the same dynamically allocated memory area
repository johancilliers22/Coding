#include <iostream>
 
int main(){
    int* pi;
    pi = new int;
 
    int* pi2;
 
    pi2 = pi;
    // now pi2 points to the same memory area as pi
 
    delete pi;
    // ok deallocated once
 
    //delete pi2;
    // not ok to deallocate again the same memory area
}